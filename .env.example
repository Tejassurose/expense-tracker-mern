PORT=5000
MONGO_URI=your_mongodb_atlas_uri_here
JWT_SECRET=your_secret_key_here


# Why .env.example? .env has your real secrets (never shared). .env.example is a safe template so other developers know what variables they need to create.